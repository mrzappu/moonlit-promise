<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout - Moonlit Promise</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="magical-particles"></div>
    
    <nav class="navbar">
        <!-- Same navbar structure -->
        <div class="nav-container">
            <div class="logo">
                <h1>Moonlit<span>Promise</span></h1>
                <div class="moon-glow"></div>
            </div>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/shop">Shop</a></li>
                <li><a href="/cart">Cart</a></li>
                <li><a href="/history">History</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/terms">Terms</a></li>
                <% if (user && user.id === 'your_admin_id_here') { %>
                    <li><a href="/admin">Admin</a></li>
                <% } %>
            </ul>
            <div class="auth-buttons">
                <% if (user) { %>
                    <div class="user-profile">
                        <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar">
                        <span><%= user.username %></span>
                        <a href="/logout" class="logout-btn">Logout</a>
                    </div>
                <% } %>
            </div>
        </div>
    </nav>

    <main>
        <div class="checkout-container">
            <h1>Complete Your Promise</h1>
            
            <div class="checkout-grid">
                <div class="order-summary">
                    <h2>Your Order</h2>
                    <% cartItems.forEach(item => { %>
                        <div class="summary-item">
                            <span><%= item.name %></span>
                            <span>$<%= item.price %></span>
                        </div>
                    <% }) %>
                    <div class="summary-total">
                        <span>Total</span>
                        <span>$<%= total %></span>
                    </div>
                </div>

                <div class="payment-section">
                    <div class="qr-section">
                        <h2>Scan to Pay</h2>
                        <img src="<%= qrImage %>" alt="Payment QR Code" class="qr-code">
                        <p class="qr-note">✨ Scan with your payment app ✨</p>
                    </div>

                    <form action="/checkout/submit" method="POST" enctype="multipart/form-data" class="payment-form">
                        <h2>Delivery Information</h2>
                        
                        <div class="form-group">
                            <label for="fullName">Full Name</label>
                            <input type="text" id="fullName" name="fullName" required placeholder="Enter your full name">
                        </div>

                        <div class="form-group">
                            <label for="phone">Phone Number</label>
                            <input type="tel" id="phone" name="phone" required placeholder="Enter your phone number">
                        </div>

                        <div class="form-group">
                            <label for="address">Complete Address</label>
                            <textarea id="address" name="address" required placeholder="Enter your full address" rows="3"></textarea>
                        </div>

                        <div class="form-group">
                            <label for="proof">Upload Payment Screenshot</label>
                            <input type="file" id="proof" name="proof" accept=".png,.jpg,.jpeg" required>
                            <p class="file-hint">Accepted formats: PNG, JPG, JPEG (Max 5MB)</p>
                        </div>

                        <button type="submit" class="btn primary submit-btn">Complete Purchase</button>
                    </form>
                </div>
            </div>
        </div>
    </main>

    <footer>
        <div class="footer-content">
            <p>Dev Rick • Moonlit Promise</p>
            <p>✨ Where every purchase is a promise under the stars ✨</p>
        </div>
    </footer>
</body>
</html>
