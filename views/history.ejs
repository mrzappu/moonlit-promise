<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order History - Moonlit Promise</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        .history-container {
            max-width: 1200px;
            margin: 4rem auto;
            padding: 0 2rem;
        }
        
        .order-card {
            background: rgba(255,255,255,0.05);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            margin-bottom: 2rem;
            border: 1px solid rgba(155,89,182,0.3);
            overflow: hidden;
        }
        
        .order-header {
            background: rgba(155,89,182,0.2);
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(155,89,182,0.3);
        }
        
        .order-number {
            font-size: 1.2rem;
            font-weight: bold;
            color: #f1c40f;
        }
        
        .order-date {
            color: rgba(255,255,255,0.6);
        }
        
        .order-body {
            padding: 2rem;
        }
        
        .order-items {
            margin-bottom: 1.5rem;
        }
        
        .order-item {
            display: flex;
            justify-content: space-between;
            padding: 0.5rem 0;
            border-bottom: 1px dashed rgba(155,89,182,0.2);
        }
        
        .order-total {
            font-size: 1.3rem;
            font-weight: bold;
            color: #f1c40f;
            text-align: right;
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 2px solid #9b59b6;
        }
        
        .status-badge {
            display: inline-block;
            padding: 0.3rem 1rem;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
            margin-right: 0.5rem;
        }
        
        .status-pending { background: rgba(241,196,15,0.2); color: #f1c40f; border: 1px solid #f1c40f; }
        .status-processing { background: rgba(52,152,219,0.2); color: #3498db; border: 1px solid #3498db; }
        .status-shipped { background: rgba(155,89,182,0.2); color: #9b59b6; border: 1px solid #9b59b6; }
        .status-delivered { background: rgba(46,204,113,0.2); color: #2ecc71; border: 1px solid #2ecc71; }
        .status-cancelled { background: rgba(231,76,60,0.2); color: #e74c3c; border: 1px solid #e74c3c; }
        
        .payment-badge {
            display: inline-block;
            padding: 0.3rem 1rem;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
            background: rgba(46,204,113,0.2);
            color: #2ecc71;
            border: 1px solid #2ecc71;
        }
        
        .payment-badge.pending {
            background: rgba(241,196,15,0.2);
            color: #f1c40f;
            border-color: #f1c40f;
        }
        
        .address-details {
            background: rgba(0,0,0,0.2);
            padding: 1rem;
            border-radius: 10px;
            margin: 1rem 0;
            font-size: 0.9rem;
            color: rgba(255,255,255,0.8);
        }
        
        .track-btn {
            background: linear-gradient(135deg, #9b59b6, #8e44ad);
            color: white;
            padding: 0.5rem 1.5rem;
            border-radius: 25px;
            text-decoration: none;
            display: inline-block;
            margin-top: 1rem;
        }
        
        .empty-state {
            text-align: center;
            padding: 4rem;
        }
        
        .empty-icon {
            font-size: 5rem;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <div class="magical-particles"></div>
    
    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">
                <img src="/moonlitpromise.png" alt="Moonlit Promise" class="logo-image">
            </div>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/shop">Shop</a></li>
                <li><a href="/cart">Cart</a></li>
                <li><a href="/history" class="active">History</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/terms">Terms</a></li>
                <% if (isAdmin) { %>
                    <li><a href="/admin">Admin</a></li>
                <% } %>
            </ul>
            <div class="auth-buttons">
                <div class="user-profile">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar">
                    <span><%= user.username %></span>
                    <a href="/logout" class="logout-btn">Logout</a>
                </div>
            </div>
        </div>
    </nav>

    <main>
        <div class="history-container">
            <h1 style="text-align: center; margin-bottom: 3rem;">✨ Your Order History ✨</h1>
            
            <% if (orders.length === 0) { %>
                <div class="empty-state">
                    <div class="empty-icon">🌙</div>
                    <h2>No orders yet</h2>
                    <p style="color: rgba(255,255,255,0.7); margin: 1rem 0 2rem;">Your first magical purchase awaits...</p>
                    <a href="/shop" class="btn primary">Start Shopping</a>
                </div>
            <% } else { %>
                <% orders.forEach(order => { %>
                    <div class="order-card">
                        <div class="order-header">
                            <span class="order-number">Order #<%= order.orderNumber %></span>
                            <span class="order-date"><%= new Date(order.createdAt).toLocaleDateString('en-IN', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                        </div>
                        <div class="order-body">
                            <!-- Status Badges -->
                            <div style="margin-bottom: 1rem;">
                                <span class="status-badge status-<%= order.orderStatus %>">
                                    <%= order.orderStatus.toUpperCase() %>
                                </span>
                                <span class="payment-badge <%= order.paymentStatus === 'completed' ? '' : 'pending' %>">
                                    <%= order.paymentMethod === 'cod' ? 'COD' : 'Online' %> - 
                                    <%= order.paymentStatus === 'completed' ? '✓ Paid' : '⏳ Pending' %>
                                </span>
                            </div>
                            
                            <!-- Order Items -->
                            <div class="order-items">
                                <h3 style="margin-bottom: 1rem;">Items</h3>
                                <% order.items.forEach(item => { %>
                                    <div class="order-item">
                                        <span>
                                            <%= item.name %> 
                                            <% if (item.quantity > 1) { %>
                                                <span style="color: #f1c40f;">(x<%= item.quantity %>)</span>
                                            <% } %>
                                        </span>
                                        <span><%= currency %><%= (item.price * (item.quantity || 1)).toLocaleString('en-IN') %></span>
                                    </div>
                                <% }) %>
                            </div>
                            
                            <!-- Total -->
                            <div class="order-total">
                                Total: <%= currency %><%= order.totalAmount.toLocaleString('en-IN') %>
                            </div>
                            
                            <!-- Delivery Address -->
                            <div class="address-details">
                                <strong>📦 Deliver to:</strong><br>
                                <%= order.fullName %><br>
                                <%= order.address %><br>
                                <%= order.pincode %><br>
                                📞 <%= order.phone %>
                            </div>
                            
                            <!-- Tracking Info -->
                            <% if (order.deliveryTracking) { %>
                                <div style="background: rgba(155,89,182,0.1); padding: 0.8rem; border-radius: 10px; margin-top: 1rem;">
                                    <strong>🚚 Tracking:</strong> <%= order.deliveryTracking %> 
                                    <% if (order.courierName) { %>(<%= order.courierName %>)<% } %>
                                </div>
                            <% } %>
                            
                            <!-- Track Button -->
                            <a href="/track/<%= order.orderNumber %>" class="track-btn">Track Order</a>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </main>

    <footer>
        <div class="footer-content">
            <p>Dev Rick • Moonlit Promise</p>
            <p>✨ Where every purchase is a promise under the stars ✨</p>
        </div>
    </footer>
</body>
</html>
