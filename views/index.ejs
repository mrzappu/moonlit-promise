<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moonlit Promise - Where Dreams Come True</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="magical-particles"></div>
    
    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">
                <h1>Moonlit<span>Promise</span></h1>
                <div class="moon-glow"></div>
            </div>
            <ul class="nav-links">
                <li><a href="/" class="active">Home</a></li>
                <li><a href="/shop">Shop</a></li>
                <li><a href="/cart">Cart</a></li>
                <li><a href="/history">History</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/terms">Terms</a></li>
                <% if (user && user.id === 'your_admin_id_here') { %>
                    <li><a href="/admin">Admin</a></li>
                <% } %>
            </ul>
            <div class="auth-buttons">
                <% if (user) { %>
                    <div class="user-profile">
                        <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar">
                        <span><%= user.username %></span>
                        <a href="/logout" class="logout-btn">Logout</a>
                    </div>
                <% } else { %>
                    <a href="/auth/discord" class="discord-btn">
                        <img src="https://cdn.prod.website-files.com/6257adef93867e50d84d30e2/663e5e2c6a25b63a75b2d082_Discord%20Logo%20-%20Wordmark%20-%20White.svg" alt="Discord">
                        Login
                    </a>
                <% } %>
            </div>
        </div>
    </nav>

    <main>
        <section class="hero">
            <div class="hero-content">
                <div class="moon-large"></div>
                <h1 class="hero-title">Moonlit Promise</h1>
                <p class="hero-subtitle">Where dreams are woven into fabric under the silver glow</p>
                <div class="hero-buttons">
                    <a href="/shop" class="btn primary">Begin Your Journey</a>
                    <a href="#featured" class="btn secondary">View Collections</a>
                </div>
            </div>
            <div class="water-reflection"></div>
        </section>

        <section id="featured" class="featured-section">
            <h2 class="section-title">Featured Under the Moonlight</h2>
            <div class="products-grid">
                <% products.forEach(product => { %>
                    <div class="product-card">
                        <div class="product-image">
                            <div class="image-placeholder" style="background: linear-gradient(135deg, #6a0dad20, #9b59b620)">
                                <span>✨</span>
                            </div>
                            <div class="product-glow"></div>
                        </div>
                        <div class="product-info">
                            <h3><%= product.name %></h3>
                            <p class="price">$<%= product.price %></p>
                            <p class="description"><%= product.description.substring(0, 60) %>...</p>
                            <form action="/cart/add/<%= product.id %>" method="POST">
                                <button type="submit" class="btn add-to-cart">Add to Cart</button>
                            </form>
                        </div>
                    </div>
                <% }) %>
            </div>
        </section>

        <section class="promise-section">
            <div class="promise-content">
                <h2>Every Garment Tells a Love Story</h2>
                <p>Handcrafted with magic dust and moonlight, our dresses capture the essence of eternal romance.</p>
                <div class="couple-silhouette">
                    <div class="silhouette"></div>
                </div>
            </div>
        </section>
    </main>

    <footer>
        <div class="footer-content">
            <p>Dev Rick • Moonlit Promise</p>
            <p>✨ Where every purchase is a promise under the stars ✨</p>
        </div>
    </footer>

    <script>
        // Magical particle effect
        document.addEventListener('DOMContentLoaded', () => {
            const particles = document.querySelector('.magical-particles');
            for (let i = 0; i < 30; i++) {
                const particle = document.createElement('div');
                particle.classList.add('particle');
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 5 + 's';
                particle.style.width = (Math.random() * 6 + 2) + 'px';
                particle.style.height = particle.style.width;
                particles.appendChild(particle);
            }
        });
    </script>
</body>
</html>
