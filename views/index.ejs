<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moonlit Promise - Premium Sports & Fashion</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="magical-particles"></div>
    
    <!-- Top Bar -->
    <div class="top-bar">
        <div class="container">
            <i class="fas fa-truck"></i> FREE SHIPPING on all orders above ₹999 | All India Delivery
        </div>
    </div>
    
    <!-- Navbar -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">
                <img src="/moonlitpromise.png" alt="Moonlit Promise">
            </div>
            <ul class="nav-links">
                <li><a href="/" class="active">Home</a></li>
                <li><a href="/shop">Shop</a></li>
                <li><a href="/cart">Cart</a></li>
                <li><a href="/history">History</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/terms">Terms</a></li>
                <% if (isAdmin) { %>
                    <li><a href="/admin">Admin</a></li>
                <% } %>
            </ul>
            <div class="auth-buttons">
                <% if (user) { %>
                    <div class="user-profile">
                        <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar">
                        <span><%= user.username %></span>
                        <a href="/logout" class="logout-btn">Logout</a>
                    </div>
                <% } else { %>
                    <a href="/auth/discord" class="discord-btn">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 127.14 96.36">
                            <path fill="#fff" d="M107.7,8.07A105.15,105.15,0,0,0,81.47,0a72.06,72.06,0,0,0-3.36,6.83A97.68,97.68,0,0,0,49,6.83,72.37,72.37,0,0,0,45.64,0,105.89,105.89,0,0,0,19.39,8.09C2.79,32.65-1.71,56.6.54,80.21h0A105.73,105.73,0,0,0,32.71,96.36,77.7,77.7,0,0,0,39.6,85.25a68.42,68.42,0,0,1-10.85-5.18c.91-.66,1.8-1.34,2.66-2a75.57,75.57,0,0,0,64.32,0c.87.71,1.76,1.39,2.66,2a68.68,68.68,0,0,1-10.87,5.19,77,77,0,0,0,6.89,11.1A105.25,105.25,0,0,0,126.6,80.22h0C129.24,53.84,122.09,29.11,107.7,8.07ZM42.45,65.69C36.18,65.69,31,60,31,53s5-12.74,11.43-12.74S54,46,53.89,53,48.84,65.69,42.45,65.69Zm42.24,0C78.41,65.69,73.25,60,73.25,53s5-12.74,11.44-12.74S96.23,46,96.12,53,91.08,65.69,84.69,65.69Z"/>
                        </svg>
                        Login
                    </a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Auto-Rotating Slider (DM3 Style) -->
    <div class="slider-container">
        <div class="slider">
            <div class="slide active" style="background-image: url('/slider/slide1.jpg');">
                <div class="slide-overlay"></div>
                <div class="slide-content">
                    <h2>Feel the thrill...</h2>
                    <h1>INDIA</h1>
                    <a href="/shop" class="slide-btn">Shop Now</a>
                </div>
            </div>
            <div class="slide" style="background-image: url('/slider/slide2.jpg');">
                <div class="slide-overlay"></div>
                <div class="slide-content">
                    <h2>Premium Quality</h2>
                    <h1>SPORTS WEAR</h1>
                    <a href="/shop" class="slide-btn">Shop Now</a>
                </div>
            </div>
            <div class="slide" style="background-image: url('/slider/slide3.jpg');">
                <div class="slide-overlay"></div>
                <div class="slide-content">
                    <h2>Magical</h2>
                    <h1>FANTASY DRESSES</h1>
                    <a href="/shop" class="slide-btn">Shop Now</a>
                </div>
            </div>
            <div class="slide" style="background-image: url('/slider/slide4.jpg');">
                <div class="slide-overlay"></div>
                <div class="slide-content">
                    <h2>Custom</h2>
                    <h1>JERSEYS</h1>
                    <a href="/shop" class="slide-btn">Shop Now</a>
                </div>
            </div>
        </div>
        <div class="slider-nav">
            <button class="slider-prev"><i class="fas fa-chevron-left"></i></button>
            <button class="slider-next"><i class="fas fa-chevron-right"></i></button>
        </div>
        <div class="slider-dots"></div>
    </div>

    <!-- OUR POPULAR CATEGORIES Section (DM3 Style) -->
    <section class="categories-section">
        <div class="container">
            <div class="section-header">
                <h2 class="section-title">OUR POPULAR CATEGORIES</h2>
            </div>
            
            <div class="categories-grid">
                <% categories.slice(0, 12).forEach(cat => { %>
                    <a href="/category/<%= cat.slug %>" class="category-card">
                        <div class="category-image">
                            <div class="emoji-icon"><%= cat.icon %></div>
                            <div class="category-glow"></div>
                        </div>
                        <h3 class="category-name"><%= cat.name %></h3>
                    </a>
                <% }) %>
            </div>
        </div>
    </section>

    <!-- NEW ARRIVALS Section -->
    <section class="products-section">
        <div class="container">
            <div class="section-header">
                <h2 class="section-title">NEW ARRIVALS</h2>
            </div>
            
            <div class="products-grid">
                <% products.slice(0, 4).forEach(product => { %>
                    <div class="product-card">
                        <div class="product-badge sale">SALE</div>
                        <div class="product-image">
                            <% if (product.image) { %>
                                <img src="/uploads/<%= product.image %>" alt="<%= product.name %>">
                            <% } else { %>
                                <div class="product-placeholder">
                                    <%= product.category === 'Cricket' ? '🏏' : 
                                        product.category === 'Football' ? '⚽' : 
                                        product.category === 'Cycling' ? '🚴' : '✨' %>
                                </div>
                            <% } %>
                        </div>
                        <div class="product-info">
                            <h3><%= product.name %></h3>
                            <div class="price-container">
                                <span class="original-price"><%= currency %><%= (product.price * 1.2).toFixed(0) %></span>
                                <span class="sale-price"><%= currency %><%= product.price.toLocaleString('en-IN') %></span>
                            </div>
                            <form action="/cart/add/<%= product.id %>" method="POST">
                                <button type="submit" class="add-to-cart-btn">Add to Cart</button>
                            </form>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </section>

    <!-- Customer Reviews Section -->
    <section class="reviews-section">
        <div class="container">
            <div class="reviews-grid">
                <div class="review-card">
                    <div class="stars">★★★★★</div>
                    <p class="review-text">Their attention to detail in design and use of premium materials ensures both style and functionality. Overall, it is a solid choice for those seeking personalised, top-notch athletic apparel.</p>
                    <p class="review-author">- Balu M</p>
                </div>
                <div class="review-card">
                    <div class="stars">★★★★★</div>
                    <p class="review-text">Diverse selection, easy navigation, and top-tier customer service make this shop a winner. Quality custom jerseys, prompt delivery, and a hassle-free experience make it my go-to for all things sports apparel. 5 stars for an unmatched shopping journey!</p>
                    <p class="review-author">- Gokul Nandu</p>
                </div>
                <div class="review-card">
                    <div class="stars">★★★★★</div>
                    <p class="review-text">This custom jerseys store kit is accurate printing and quality materials are available cheaper than other stores. Best jersey printing and sports shop.</p>
                    <p class="review-author">- M Unais</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer (DM3 Style) -->
    <footer class="footer">
        <div class="container">
            <div class="footer-grid">
                <div class="footer-col">
                    <img src="/moonlitpromise.png" alt="Moonlit Promise" class="footer-logo">
                    <p>Our Social Links:</p>
                    <div class="social-links">
                        <a href="#"><i class="fab fa-facebook"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-linkedin"></i></a>
                    </div>
                </div>
                <div class="footer-col">
                    <ul class="footer-links">
                        <li><a href="/privacy-policy">Privacy Policy</a></li>
                        <li><a href="/exchange-return">Exchange & Return Policy</a></li>
                        <li><a href="/terms">Terms and conditions</a></li>
                        <li><a href="/faq">FAQ</a></li>
                        <li><a href="/about">About us</a></li>
                        <li><a href="/contact">Contact Us</a></li>
                        <li><a href="/blogs">Blogs</a></li>
                    </ul>
                </div>
                <div class="footer-col">
                    <h4>Email:</h4>
                    <p><a href="mailto:dm3care@gmail.com">dm3care@gmail.com</a></p>
                    <h4>Address:</h4>
                    <p>
                        1st floor, F 14/1,<br>
                        Near Saraswathi construction,<br>
                        F street, Panickers lane,<br>
                        Shasthamangalam,<br>
                        Trivandrum.
                    </p>
                </div>
            </div>
            <div class="footer-bottom">
                <p>© Copyright 2025 Moonlit Promise | All Rights Reserved. Designed by Dev Rick</p>
            </div>
        </div>
    </footer>

    <script>
        // Auto-rotating slider
        document.addEventListener('DOMContentLoaded', function() {
            const slides = document.querySelectorAll('.slide');
            const prevBtn = document.querySelector('.slider-prev');
            const nextBtn = document.querySelector('.slider-next');
            const dotsContainer = document.querySelector('.slider-dots');
            let currentSlide = 0;
            let slideInterval;

            // Create dots
            slides.forEach((_, index) => {
                const dot = document.createElement('span');
                dot.classList.add('dot');
                if (index === 0) dot.classList.add('active');
                dot.addEventListener('click', () => goToSlide(index));
                dotsContainer.appendChild(dot);
            });

            const dots = document.querySelectorAll('.dot');

            function showSlide(index) {
                slides.forEach(slide => slide.classList.remove('active'));
                dots.forEach(dot => dot.classList.remove('active'));
                
                slides[index].classList.add('active');
                dots[index].classList.add('active');
                currentSlide = index;
            }

            function nextSlide() {
                let next = (currentSlide + 1) % slides.length;
                showSlide(next);
            }

            function prevSlide() {
                let prev = (currentSlide - 1 + slides.length) % slides.length;
                showSlide(prev);
            }

            function goToSlide(index) {
                showSlide(index);
                resetInterval();
            }

            function resetInterval() {
                clearInterval(slideInterval);
                slideInterval = setInterval(nextSlide, 5000);
            }

            if (slides.length > 0) {
                showSlide(0);
                slideInterval = setInterval(nextSlide, 5000);
                
                if (nextBtn) nextBtn.addEventListener('click', () => {
                    nextSlide();
                    resetInterval();
                });
                
                if (prevBtn) prevBtn.addEventListener('click', () => {
                    prevSlide();
                    resetInterval();
                });
            }

            // Particle effect
            const particles = document.querySelector('.magical-particles');
            for (let i = 0; i < 30; i++) {
                const particle = document.createElement('div');
                particle.classList.add('particle');
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 5 + 's';
                particle.style.width = (Math.random() * 6 + 2) + 'px';
                particle.style.height = particle.style.width;
                particles.appendChild(particle);
            }
        });
    </script>
</body>
</html>
