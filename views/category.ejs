<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= category.name %> - Moonlit Promise</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="magical-particles"></div>
    
    <div class="top-bar">
        <div class="container">
            <i class="fas fa-truck"></i> FREE SHIPPING on all orders above ₹999
        </div>
    </div>
    
    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">
                <img src="/moonlitpromise.png" alt="Moonlit Promise">
            </div>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/shop" class="active">Shop</a></li>
                <li><a href="/cart">Cart</a></li>
                <li><a href="/history">History</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/terms">Terms</a></li>
                <% if (isAdmin) { %>
                    <li><a href="/admin">Admin</a></li>
                <% } %>
            </ul>
            <div class="auth-buttons">
                <% if (user) { %>
                    <div class="user-profile">
                        <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar">
                        <span><%= user.username %></span>
                        <a href="/logout" class="logout-btn">Logout</a>
                    </div>
                <% } else { %>
                    <a href="/auth/discord" class="discord-btn">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 127.14 96.36">
                            <path fill="#fff" d="M107.7,8.07A105.15,105.15,0,0,0,81.47,0a72.06,72.06,0,0,0-3.36,6.83A97.68,97.68,0,0,0,49,6.83,72.37,72.37,0,0,0,45.64,0,105.89,105.89,0,0,0,19.39,8.09C2.79,32.65-1.71,56.6.54,80.21h0A105.73,105.73,0,0,0,32.71,96.36,77.7,77.7,0,0,0,39.6,85.25a68.42,68.42,0,0,1-10.85-5.18c.91-.66,1.8-1.34,2.66-2a75.57,75.57,0,0,0,64.32,0c.87.71,1.76,1.39,2.66,2a68.68,68.68,0,0,1-10.87,5.19,77,77,0,0,0,6.89,11.1A105.25,105.25,0,0,0,126.6,80.22h0C129.24,53.84,122.09,29.11,107.7,8.07ZM42.45,65.69C36.18,65.69,31,60,31,53s5-12.74,11.43-12.74S54,46,53.89,53,48.84,65.69,42.45,65.69Zm42.24,0C78.41,65.69,73.25,60,73.25,53s5-12.74,11.44-12.74S96.23,46,96.12,53,91.08,65.69,84.69,65.69Z"/>
                        </svg>
                        Login
                    </a>
                <% } %>
            </div>
        </div>
    </nav>

    <main>
        <div class="category-header">
            <div class="container">
                <div class="category-icon"><%= category.icon %></div>
                <h1><%= category.name %></h1>
            </div>
        </div>

        <div class="container">
            <% if (products.length === 0) { %>
                <div class="empty-state">
                    <div class="empty-icon">📦</div>
                    <h2>No products found</h2>
                    <p>Check back soon for new arrivals in this category!</p>
                    <a href="/shop" class="btn btn-primary">Browse All</a>
                </div>
            <% } else { %>
                <div class="products-grid">
                    <% products.forEach(product => { %>
                        <div class="product-card">
                            <% if (product.stock < 5) { %>
                                <div class="product-badge low-stock">Low Stock</div>
                            <% } %>
                            <div class="product-image">
                                <% if (product.image) { %>
                                    <img src="/uploads/<%= product.image %>" alt="<%= product.name %>">
                                <% } else { %>
                                    <div class="product-placeholder"><%= category.icon %></div>
                                <% } %>
                            </div>
                            <div class="product-info">
                                <h3><%= product.name %></h3>
                                <p class="price"><%= currency %><%= product.price.toLocaleString('en-IN') %></p>
                                <p class="description"><%= product.description.substring(0, 60) %>...</p>
                                <form action="/cart/add/<%= product.id %>" method="POST">
                                    <button type="submit" class="add-to-cart-btn">Add to Cart</button>
                                </form>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } %>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <div class="footer-bottom">
                <p>© Copyright 2025 Moonlit Promise | All Rights Reserved. Designed by Dev Rick</p>
            </div>
        </div>
    </footer>

    <style>
        .category-header {
            background: linear-gradient(135deg, #1a1a3a, #2a1a4a);
            padding: 3rem 0;
            text-align: center;
            border-bottom: 1px solid rgba(155,89,182,0.3);
        }
        .category-icon {
            font-size: 5rem;
            margin-bottom: 1rem;
        }
        .category-header h1 {
            font-size: 2.5rem;
            color: #f1c40f;
        }
        .empty-state {
            text-align: center;
            padding: 4rem 0;
        }
        .empty-icon {
            font-size: 5rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }
        .btn-primary {
            display: inline-block;
            padding: 1rem 2rem;
            background: linear-gradient(135deg, #9b59b6, #8e44ad);
            color: white;
            text-decoration: none;
            border-radius: 10px;
            margin-top: 1rem;
        }
        .products-grid {
            padding: 3rem 0;
        }
    </style>
</body>
</html>
