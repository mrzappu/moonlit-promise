<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation - Moonlit Promise</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700;900&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .confirmation-container {
            max-width: 800px;
            margin: 4rem auto;
            padding: 0 2rem;
        }

        .success-animation {
            text-align: center;
            margin-bottom: 2rem;
        }

        .checkmark-circle {
            width: 150px;
            height: 150px;
            position: relative;
            display: inline-block;
            vertical-align: top;
            margin-left: auto;
            margin-right: auto;
        }

        .checkmark-circle .background {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            background: linear-gradient(135deg, #9b59b6, #8e44ad);
            position: absolute;
        }

        .checkmark-circle .checkmark {
            border-radius: 50%;
            width: 150px;
            height: 150px;
            display: block;
            stroke-width: 2;
            stroke: #fff;
            stroke-miterlimit: 10;
            box-shadow: inset 0px 0px 0px #7ac142;
            animation: fill .4s ease-in-out .4s forwards, scale .3s ease-in-out .9s both;
        }

        .checkmark-circle .checkmark-circle {
            stroke-dasharray: 166;
            stroke-dashoffset: 166;
            stroke-width: 2;
            stroke-miterlimit: 10;
            stroke: #fff;
            fill: none;
            animation: stroke 0.6s cubic-bezier(0.65, 0, 0.45, 1) forwards;
        }

        .checkmark-circle .checkmark-check {
            transform-origin: 50% 50%;
            stroke-dasharray: 48;
            stroke-dashoffset: 48;
            animation: stroke 0.3s cubic-bezier(0.65, 0, 0.45, 1) 0.8s forwards;
        }

        @keyframes stroke {
            100% { stroke-dashoffset: 0; }
        }

        @keyframes scale {
            0%, 100% { transform: none; }
            50% { transform: scale3d(1.1, 1.1, 1); }
        }

        @keyframes fill {
            100% { box-shadow: inset 0px 0px 0px 100px #9b59b6; }
        }

        .confirmation-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 2rem;
            border: 1px solid rgba(155, 89, 182, 0.3);
            margin-bottom: 2rem;
        }

        .order-number {
            text-align: center;
            margin-bottom: 2rem;
        }

        .order-number h2 {
            font-size: 1.2rem;
            color: rgba(255, 255, 255, 0.7);
            margin-bottom: 0.5rem;
        }

        .order-number .number {
            font-size: 2rem;
            color: #f1c40f;
            font-weight: 700;
            letter-spacing: 2px;
        }

        .status-badge {
            display: inline-block;
            padding: 0.5rem 2rem;
            background: rgba(241, 196, 15, 0.2);
            border: 1px solid #f1c40f;
            border-radius: 30px;
            color: #f1c40f;
            font-weight: 600;
            margin: 1rem 0;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin: 2rem 0;
        }

        .info-section {
            background: rgba(255, 255, 255, 0.02);
            padding: 1.5rem;
            border-radius: 15px;
            border: 1px solid rgba(155, 89, 182, 0.2);
        }

        .info-section h3 {
            color: #f1c40f;
            margin-bottom: 1rem;
            font-size: 1.2rem;
        }

        .info-section p {
            color: rgba(255, 255, 255, 0.8);
            line-height: 1.6;
            margin-bottom: 0.3rem;
        }

        .items-list {
            margin: 2rem 0;
        }

        .items-list h3 {
            color: #f1c40f;
            margin-bottom: 1rem;
        }

        .item-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
            border-bottom: 1px solid rgba(155, 89, 182, 0.2);
        }

        .item-row:last-child {
            border-bottom: none;
        }

        .item-name {
            flex: 2;
            font-weight: 500;
        }

        .item-quantity {
            flex: 1;
            text-align: center;
            color: rgba(255, 255, 255, 0.7);
        }

        .item-price {
            flex: 1;
            text-align: right;
            color: #f1c40f;
            font-weight: 600;
        }

        .total-row {
            display: flex;
            justify-content: space-between;
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 2px solid #9b59b6;
            font-size: 1.3rem;
            font-weight: 700;
            color: #f1c40f;
        }

        .action-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            margin-top: 2rem;
        }

        .btn {
            padding: 1rem 2rem;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, #9b59b6, #8e44ad);
            color: white;
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            border: 1px solid #9b59b6;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(155, 89, 182, 0.4);
        }

        .whatsapp-share {
            background: #25D366;
            color: white;
            border: none;
        }

        .whatsapp-share:hover {
            background: #128C7E;
        }

        @media (max-width: 768px) {
            .info-grid {
                grid-template-columns: 1fr;
            }
            
            .action-buttons {
                flex-direction: column;
            }
            
            .btn {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <div class="magical-particles"></div>
    
    <!-- Top Bar -->
    <div class="top-bar">
        <div class="container">
            <i class="fas fa-truck"></i> FREE SHIPPING on all orders above ₹999 | All India Delivery
        </div>
    </div>
    
    <!-- Navbar -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">
                <img src="/moonlitpromise.png" alt="Moonlit Promise">
            </div>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/shop">Shop</a></li>
                <li><a href="/cart">Cart</a></li>
                <li><a href="/history">History</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/terms">Terms</a></li>
                <% if (isAdmin) { %>
                    <li><a href="/admin">Admin</a></li>
                <% } %>
            </ul>
            <div class="auth-buttons">
                <% if (user) { %>
                    <div class="user-profile">
                        <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="Avatar">
                        <span><%= user.username %></span>
                        <a href="/logout" class="logout-btn">Logout</a>
                    </div>
                <% } else { %>
                    <a href="/auth/discord" class="discord-btn">
                        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 127.14 96.36">
                            <path fill="#fff" d="M107.7,8.07A105.15,105.15,0,0,0,81.47,0a72.06,72.06,0,0,0-3.36,6.83A97.68,97.68,0,0,0,49,6.83,72.37,72.37,0,0,0,45.64,0,105.89,105.89,0,0,0,19.39,8.09C2.79,32.65-1.71,56.6.54,80.21h0A105.73,105.73,0,0,0,32.71,96.36,77.7,77.7,0,0,0,39.6,85.25a68.42,68.42,0,0,1-10.85-5.18c.91-.66,1.8-1.34,2.66-2a75.57,75.57,0,0,0,64.32,0c.87.71,1.76,1.39,2.66,2a68.68,68.68,0,0,1-10.87,5.19,77,77,0,0,0,6.89,11.1A105.25,105.25,0,0,0,126.6,80.22h0C129.24,53.84,122.09,29.11,107.7,8.07ZM42.45,65.69C36.18,65.69,31,60,31,53s5-12.74,11.43-12.74S54,46,53.89,53,48.84,65.69,42.45,65.69Zm42.24,0C78.41,65.69,73.25,60,73.25,53s5-12.74,11.44-12.74S96.23,46,96.12,53,91.08,65.69,84.69,65.69Z"/>
                        </svg>
                        Login
                    </a>
                <% } %>
            </div>
        </div>
    </nav>

    <main>
        <div class="confirmation-container">
            <!-- Success Animation -->
            <div class="success-animation">
                <div class="checkmark-circle">
                    <div class="background"></div>
                    <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
                        <circle class="checkmark-circle" cx="26" cy="26" r="25" fill="none"/>
                        <path class="checkmark-check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8"/>
                    </svg>
                </div>
            </div>

            <!-- Order Number -->
            <div class="order-number">
                <h2>Order Placed Successfully!</h2>
                <div class="number"><%= order.orderNumber %></div>
                <div class="status-badge">
                    <i class="fas fa-clock"></i> Status: <%= order.orderStatus %>
                </div>
            </div>

            <!-- Main Confirmation Card -->
            <div class="confirmation-card">
                <h2 style="color: #f1c40f; margin-bottom: 1.5rem;">Thank You for Your Order!</h2>
                <p style="color: rgba(255,255,255,0.8); line-height: 1.6;">
                    Your order has been received and is being processed. You will receive an email confirmation shortly with your order details and tracking information once your order ships.
                </p>
            </div>

            <!-- Order Details Grid -->
            <div class="info-grid">
                <!-- Shipping Address -->
                <div class="info-section">
                    <h3><i class="fas fa-map-marker-alt"></i> Shipping Address</h3>
                    <p><strong><%= order.fullName %></strong></p>
                    <p><%= order.address %></p>
                    <p><%= order.pincode %></p>
                    <p><i class="fas fa-phone"></i> <%= order.phone %></p>
                </div>

                <!-- Payment Method -->
                <div class="info-section">
                    <h3><i class="fas fa-credit-card"></i> Payment Method</h3>
                    <p><strong><%= order.paymentMethod === 'cod' ? 'Cash on Delivery' : 'Online Payment' %></strong></p>
                    <% if (order.paymentMethod === 'cod') { %>
                        <p>Pay ₹<%= order.totalAmount.toLocaleString('en-IN') %> at the time of delivery</p>
                        <p style="color: #f1c40f;">✓ Phone Verified</p>
                    <% } else { %>
                        <p>Payment Status: <span style="color: #f1c40f;"><%= order.paymentStatus %></span></p>
                    <% } %>
                </div>

                <!-- Order Summary -->
                <div class="info-section">
                    <h3><i class="fas fa-box"></i> Order Summary</h3>
                    <p>Items: <%= items.length %></p>
                    <p>Subtotal: ₹<%= order.totalAmount.toLocaleString('en-IN') %></p>
                    <p>Shipping: <%= order.totalAmount >= 999 ? 'FREE' : '₹99' %></p>
                    <p style="color: #f1c40f; font-size: 1.2rem; margin-top: 0.5rem;">
                        Total: ₹<%= (order.totalAmount + (order.totalAmount >= 999 ? 0 : 99)).toLocaleString('en-IN') %>
                    </p>
                </div>
            </div>

            <!-- Items List -->
            <div class="confirmation-card">
                <div class="items-list">
                    <h3>Order Items</h3>
                    <% items.forEach(item => { %>
                        <div class="item-row">
                            <div class="item-name">
                                <%= item.name %>
                                <% if (item.quantity > 1) { %>
                                    <span style="color: #f1c40f; font-size: 0.9rem;">(x<%= item.quantity %>)</span>
                                <% } %>
                            </div>
                            <div class="item-quantity">Qty: <%= item.quantity %></div>
                            <div class="item-price">₹<%= (item.price * item.quantity).toLocaleString('en-IN') %></div>
                        </div>
                    <% }) %>
                </div>
            </div>

            <!-- Action Buttons -->
            <div class="action-buttons">
                <a href="/history" class="btn btn-primary">
                    <i class="fas fa-history"></i> View Order History
                </a>
                <a href="/track/<%= order.orderNumber %>" class="btn btn-secondary">
                    <i class="fas fa-truck"></i> Track Order
                </a>
                <a href="/shop" class="btn btn-secondary">
                    <i class="fas fa-shopping-bag"></i> Continue Shopping
                </a>
            </div>

            <!-- WhatsApp Share -->
            <div style="text-align: center; margin-top: 2rem;">
                <a href="https://wa.me/?text=Thank%20you%20for%20ordering%20from%20Moonlit%20Promise!%20Your%20order%20number%20is%20<%= order.orderNumber %>" 
                   target="_blank" 
                   class="btn whatsapp-share">
                    <i class="fab fa-whatsapp"></i> Share on WhatsApp
                </a>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer" style="background: #0a0a0f; padding: 2rem 0; margin-top: 4rem; border-top: 1px solid rgba(155,89,182,0.3);">
        <div class="container">
            <div style="text-align: center; color: rgba(255,255,255,0.5);">
                <p>© 2025 Moonlit Promise. Thank you for shopping with us!</p>
            </div>
        </div>
    </footer>

    <script>
        // Particle effect
        document.addEventListener('DOMContentLoaded', () => {
            const particles = document.querySelector('.magical-particles');
            for (let i = 0; i < 20; i++) {
                const particle = document.createElement('div');
                particle.classList.add('particle');
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 5 + 's';
                particle.style.width = (Math.random() * 4 + 1) + 'px';
                particle.style.height = particle.style.width;
                particles.appendChild(particle);
            }
        });
    </script>
</body>
</html>
